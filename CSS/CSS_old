/* General styles 
  (the styles used everywhere in the page (like the Body) or styles which are used in more than 2 of the modules bellow (h2, h3, h4, h5, h6, p, a, ul, li))
*/

* {
  /* Layout Declarations */
  box-sizing: border-box;
}

.masthead-heading {
  font-size: 3em;
}
}
body {
  /* Typography Declarations */
  
  color: #222222;
  font-size: 1em;
  font-family: "Open Sans", "Helvetica Neue", sans-serif;
}
a {
  /* Typography Declrations */
  
  font-weight: bold;
  text-decoration: none;
  color: #FFCF33;
}
a:hover {
  /* Tyopography Declarations */
  
  color: #FFE799;
}
a:visited {
  /* Typography Declarations */
  
  color: #D1A000;
}
.content-footer,
.masthead-heading,
.masthead-intro {
  text-align: center;
}
h1 {
  color: #00A80F;
}
h2 {
  color: #00A80F;
  padding: 1em 0;
  font-size: 1.3em;
}
h3 {
  color: #00A80F;
  padding-top: 1em;
  padding-bottom: .5em;
  font-size: 1em;
}
ul {
  margin-left: 3em;
  padding-top: 1em;
  padding-bottom: 1em;
}
/* Header styles 
  (all the styles inside the < header > or < nav > tags) 
*/

.masthead {
  padding: 6em 0;
  background-image: url('https://images.unsplash.com/photo-1428278953961-a8bc45e05f72?q=80&fm=jpg&s=24f29a71c4de9b922e7ee2cb5ee46f4b');
  background-repeat: no-repeat;
  background-size: cover;
  border-top: solid 1em #00A80F;
}
.masthead-intro {
  /* Layout Declarations */
  
  margin-bottom: 0.1em;
  /* Typography Declarations */
  
  font-family: "Gentium Book Basic", Georgia, serif;
  font-size: 2em;
}
.masthead-heading {
  /* Layout Declarations */
  
  margin-top: -0.2em;
  /* Typography Declarations */
  
  font-family: "Open Sans", "Helvetica Neue", sans-serif;
  font-size: 6em;
  letter-spacing: -0.02em;
  font-style: normal;
  font-weight: bold;
  font-size: 6em;
  letter-spacing: -0.02em;
  text-transform: uppercase;
  text-shadow: 0px 3px 0px #FFCF33;
}
/* Content styles 
  (all the styles inside the < sections > or < aside > tags)
*/

.introduction-section,
.location-section,
.questions-section {
  max-width: 38em;
  margin-left: auto;
  margin-right: auto;
  margin-top: 2em;
  margin-bottom: 2em;
}
.introduction-section > p,
.location-section > p,
.content-footer > p {
  /* Typography Declarations */
  
  font-weight: 300;
  letter-spacing: 0.05em;
}
.questions-section > h2 {
  margin: 2em 0 1em 0;
}
section > h1 {
  margin-top: 2em;
}
/* Footer styles 
  (all the styles inside the < footer > tag)
*/

.content-footer {
  /* Red, Green, Blue: #RRGGBB */
  text-align: center;
  background-color: #00A80F;
  border-radius: 10px;
  padding: 3em;
  border-bottom: solid .5em #FFCF33;
  margin: .5em;
}
.content-footer > p {
  color: white;
}
.social > li {
  display: inline-block;
  text-align: center;
  padding-left: 1em;
  padding-right: 1em;
}
/* Responsive styles 
  (the very last module in the CSS, contains all the media queries and it is dealing with the compatibility with mobiles and tablets)
*/

@media only screen and (max-width: 500px) {
    .masthead {
        padding: 3em 0;
    }
    .masthead-heading {
        font-size: 3em;
    }
}